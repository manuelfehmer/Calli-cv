# -*- mode: python -*-
a = Analysis(['Calli-cv.py'],
             pathex=['C:\\Users\\Manuel\\Documents\\GitHub\\Calli-cv'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='Calli-cv.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='Calli-cv')
